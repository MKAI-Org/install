# Claude Code CLI（用户说的 Cloud Code）+ 可选桌面客户端
#
#   Mac/Linux: ./install.sh
#   Windows:   .\install.ps1
#   桌面客户端: ./install-app.sh   .\install-app.ps1
#   配 key:     ./configure.sh --key ... [--base-url ...]
#
# 不需要 Node。国内把 packages/ 拷过去就能装。
